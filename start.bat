@echo off
rem Переходим в папку, где лежит сам этот .bat-файл — не важно, откуда
rem кликнули, скрипт запустится с правильной рабочей директорией
rem (иначе config.txt и vkrpc.log могут создаться не там, где нужно).
cd /d "%~dp0"

python main.py

rem Если скрипт упал с ошибкой или просто завершился — окно не
rem закроется само, будет видно, что произошло, до нажатия любой клавиши.
pause
